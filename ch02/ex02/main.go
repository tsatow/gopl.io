package main
/*
気が乗らないので後回し
import (
	"os"
	"fmt"
	"bufio"
	"strconv"
	"flag"
)

var (
	t = flag.Bool("t", false, "templature")
	l = flag.Bool("l", false, "length")
	w = flag.Bool("w", false, "weight")
)

func main() {
	if !(*t || *l || *w) {
		*t, *l, *w = true, true, true
	}

	if len(os.Args) <= 1 {
		sc := bufio.NewScanner(os.Stdin)

		for sc.Scan() {
			target := sc.Text()

		}
	} else {
	}
}

 */